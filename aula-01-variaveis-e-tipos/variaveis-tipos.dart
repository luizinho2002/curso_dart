void main() {

  // Cria uma variável chamada "nome" do tipo String (texto) e guarda o valor "Luiz Fernando" dentro dela
  String nome = 'Luiz Fernando';
  
  // Imprime o valor da variável "nome" na tela
  print(nome);

  // Criar uma variável chamada "idade" do tipo int (número) e guarda o valor 22 dentro dela
  int idade = 22;
 
  // Imprime o valor da variável "idade" na tela.
  print(idade);

  // Criar uma variável chamada "programador" do tipo bool (verdadeiro ou falso) e guarda o valor "true" (verdadeiro) dentro dela
  bool programador = true;
  
  // Imprime o valor da variável "programador" na tela.
  print(programador);

  //  Cria uma lista chamada "palavras" que contém duas Strings: "Luiz" e "Fernando"
  List<String> palavras = ['Luiz', 'Fernando'];
  
  // Imprime o primeiro elemento da lista (índice 0) e o segundo elemento da lista (índice 1) na tela, separador por um hífen.
  print('${palavras[0]} - ${palavras[1]}');
}